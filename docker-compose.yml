version: '3'

services:
  banco:
    image: mariadb:latest
    environment:
      - MARIADB_ROOT_PASSWORD=ROOT@123
      - MARIADB_DATABASE=ponto
      - MARIADB_USER=ponto
      - MARIADB_USER_PASSWORD=PONTO@123